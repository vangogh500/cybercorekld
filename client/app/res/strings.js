// --------------- Titles ---------------------------------

/**
 * Title for {@link AuthHomePage}
 * @typedef {String} AUTH_HOME_TITLE
 */
export const AUTH_HOME_TITLE = "CyberCore KLD - Home"

// --------------- URLS ---------------------------------

/**
 * Link for {@link AuthHomePage}
 * @typedef {String} AUTH_HOME_URL
 */
export const AUTH_HOME_URL = "/auth/home"

/**
 * Link for {@link AuthLoginPage}
 * @typedef {String} AUTH_LOGIN_URL
 */
export const AUTH_LOGIN_URL = "/auth"
/**
 * Link for authorized pages
 * @typedef {String} AUTH_URL
 */
export const AUTH_URL = "/auth"

/**
 * Link for {@link LadderHomePage}
 * @typedef {String} LADDER_HOME_URL
 */
 export const LADDER_HOME_URL = "/ladder"

 /**
  * Link for {@link TournamentHomePage}
  * @typedef {String} TOURNAMENT_HOME_URL
  */
  export const TOURNAMENT_HOME_URL = "/tournaments"

  /**
   * Link for {@link UserHomePage}
   * @typedef {String} USER_HOME_URL
   */
   export const USER_HOME_URL = "/users"


// --------------- FORM LABELS ---------------------------------
/**
 * Username prompt for {@link AuthForm}
 * @typedef {String} AUTH_FORM_USERNAME_PROMPT
 */
export const AUTH_FORM_USERNAME_PROMPT = "Username"
/**
 * Password prompt for {@link AuthForm}
 * @typedef {String} AUTH_FORM_PASSWORD_PROMPT
 */
export const AUTH_FORM_PASSWORD_PROMPT = "Password"
/**
 * Login button label {@link AuthForm}
 * @typedef {String} LOGIN_BUTTON_LABEL
 */
export const LOGIN_BUTTON_LABEL = "Login"

/**
 * Logout button label {@link AuthForm}
 * @typedef {String} LOGOUT_BUTTON_LABEL
 */
 export const LOGOUT_BUTTON_LABEL = "Logout"

// --------------- MSGS ---------------------------------
/**
 * 200 response for {@link AuthForm}
 * @typedef {String} AUTH_FORM_200_MESSAGE
 */
export const AUTH_FORM_200_MESSAGE = "Success! Redirecting..."
/**
 * 401 response for {@link AuthForm}
 * @typedef {String} AUTH_FORM_401_MESSAGE
 */
export const AUTH_FORM_401_MESSAGE = "Invalid login."
/**
 * 500 response for {@link AuthForm}
 * @typedef {String} AUTH_FORM_500_MESSAGE
 */
export const AUTH_FORM_500_MESSAGE = "Oops, something went wrong!"
/**
 * Invalid credentials prompt for React components from {@link withAuth}
 * @typedef {String} AUTH_INVALID_PROMPT
 */
 export const AUTH_INVALID_PROMPT = "Unauthorized. Redirecting.."
